\NeedsTeXFormat{LaTeX2e}
\usepackage[margin=1in]{geometry}
\usepackage{enumerate}
\usepackage{verbatim}
\usepackage{hyperref}
\hypersetup{
    colorlinks=true,
    linkcolor=black,
    urlcolor=blue
}


\newcommand\Quote[1]{\textit{,,#1''}}

%oznake/opozorila, da nekaj manjka, 
\newcommand\fixme[0]{
{${\color{red}\bullet}$}
}
\newcommand\todo[0]{
{${\color{red}\bullet}$}
}

%predloge za tikz
\newenvironment{tikzturing}{
\pgfkeys{tikz/.cd, node distance=-0.075mm, thick,
	%za en trak
	start chain=trak,
	%za več trakov
	start chain=trak1, start chain=trak2, start chain=trak3,
	start chain=trak4, start chain=trak5, 
	%celice
	cell/.style={draw, very thin, minimum size=0.6cm, on chain=trak},
	cell1/.style={cell, on chain=trak1, minimum width=1cm},
	cell2/.style={cell, on chain=trak2, minimum width=1cm},
	cell3/.style={cell, on chain=trak3, minimum width=1cm},
	cell4/.style={cell, on chain=trak4, minimum width=1cm},
	cell5/.style={cell, on chain=trak5, minimum width=1cm},
	selected/.style={ultra thick},
	end/.style={ultra thick, draw=white},
	%nadzorna enota
	head/.style={draw, minimum width=2cm, minimum height=1.2cm}
}
\begin{tikzpicture}}{\end{tikzpicture}}

%neurejeni del zapiskov
\def\Neurejeno{
\pagebreak
{\Huge od tu naprej je neurejeni del zapiskov.. halp!\\}
\color{gray}}

%ker sem len za pisat, oz. na predavanjih je treba bit hiter :)
%subsection + hint pagebreak + label
\newcommand\chap[1]{%
\chapter{#1}\label{chap:#1}}
\def\sect{
\pagebreak[3]\section
}
\def\subsect{
\pagebreak[2]\subsection
}
\def\subsubsect{
\pagebreak[1]\subsubsection
}

\newcommand\fnurl[2]{%
  \href{#2}{#1}\footnote{\url{#2}}%
}
\newcommand\br{%? any better ideas? check \par
 \ \\ \ \\%
}
\newenvironment{items}{
\begin{itemize}
	\setlength{\itemsep}{2pt}
	\setlength{\parskip}{0pt}
	\setlength{\parsep}{0pt}
	\setlength{\topsep}{0pt}
}{\end{itemize}}
\newenvironment{enum}[1]{
\begin{enumerate}[#1]
	\setlength{\itemsep}{2pt}
	\setlength{\parskip}{0pt}
	\setlength{\parsep}{0pt}
	\setlength{\topsep}{0pt}
}{\end{enumerate}}

%morda možen način: neki: \marginpar{}
\newcommand\odmakni[2]{
\begin{enum}{{#1}:}
	\item {#2}
\end{enum}}
\newcommand\Odmakni[2]{
\begin{enum}{\bf {#1}:}
	\item {#2}
\end{enum}}

\newcommand\Def[1]{\Odmakni{Def.}{#1}}
\newcommand\Primer[1]{\Odmakni{Primer}{#1}}
\newcommand\Trditev[1]{\Odmakni{Trditev}{#1}}
\newcommand\Dokaz[1]{\Odmakni{Dokaz}{#1}}
\newcommand\Izrek[1]{\Odmakni{Izrek}{#1}}
\newcommand\Posledica[1]{\Odmakni{Posledica}{#1}}
\newcommand\Velja[1]{\Odmakni{Velja}{#1}}
\newcommand\Lema[1]{\Odmakni{Lema}{#1}}
\newcommand\Ideja[1]{\Odmakni{Ideja}{#1}}
\newcommand\Povzetek[1]{\Odmakni{Povzetek}{#1}}

\def\PoDef{\overset{def}{=}}


\newenvironment{primeri}{
{\subsection*{Primeri:}}
\begin{enum}{\bf Pr{i}mer 1:}}
{\end{enum}}

